const refs = {
    form: document.querySelector('login-form'),
    email: document.querySelector('.input'),
    password: document.querySelector('form input'),
    button: document.querySelector('button')
}

console.log(refs.email);
console.log(refs.password);